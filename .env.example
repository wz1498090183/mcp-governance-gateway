# 复制为 .env 后使用（.env 已被 .gitignore 忽略，禁止提交真实密钥）

# JWT 密钥（网关验证、scripts/gen_token.py 签发、scripts/demo.sh 签发共用）
JWT_SECRET=dev-secret-change-me-0123456789abcdef

# Redis 地址（容器网络内使用服务名 redis）
REDIS_URL=redis://redis:6379/0
